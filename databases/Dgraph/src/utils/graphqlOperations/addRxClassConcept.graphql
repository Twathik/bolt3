mutation addRXClassConcept ($input: [AddRXClassConceptInput!]!, $upsert: Boolean  ) {
  addRXClassConcept(
    input: $input
    upsert: $upsert
  ) {
    rXClassConcept {
      id
    }
    numUids
  }
}
