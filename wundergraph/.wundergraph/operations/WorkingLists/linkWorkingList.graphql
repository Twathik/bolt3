mutation ($id: String!, $linkId: String!) {
  mainDb_updateOneWorkingList(
    where: { id: $id }
    data: {
      linked: { set: true }
      linkId: { set: $linkId }
      modalityExamStatus: { set: INPROGRESS }
    }
  ) {
    id
  }
}
