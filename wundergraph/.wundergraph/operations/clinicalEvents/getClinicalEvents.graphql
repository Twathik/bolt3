query ($patientId: String!) {
  mainDb_clinicalEvents(where: { patientId: { equals: $patientId } }) {
    id
    eventType
    updatedAt
    createdReport
    report
    empty
    dicomId
    dicom
    user {
      fullName
    }
  }
}
