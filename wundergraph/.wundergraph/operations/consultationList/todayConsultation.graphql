query ($day: Int!, $month: Int!, $year: Int!) {
  mainDb_findFirstConsultation(
    where: {
      AND: [
        { day: { equals: $day } }
        { month: { equals: $month } }
        { year: { equals: $year } }
      ]
    }
  ) {
    id
  }
}
