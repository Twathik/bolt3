mutation ($id: String!, $documentTemplateConfiguration: String!) {
  mainDb_updateOneSetting(
    where: { id: $id }
    data: {
      documentTemplateConfiguration: { set: $documentTemplateConfiguration }
    }
  ) {
    id
  }
}
