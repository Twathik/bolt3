mutation (
  $id: String!
  $documentData: String
  $userId: String! @fromClaim(name: USERID)
) {
  mainDb_updateOnePatient(
    where: { id: $id }
    userId: $userId
    data: { documentData: { set: $documentData } }
  ) {
    ...PatientFragment
  }
}
