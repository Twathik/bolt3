mutation (
  $id: String!
  $clinicalData: String
  $userId: String! @fromClaim(name: USERID)
) {
  mainDb_updateOnePatient(
    where: { id: $id }
    userId: $userId
    data: { clinicalData: { set: $clinicalData } }
  ) {
    ...PatientFragment
  }
}
