query ($consultationDate: String!) {
  mainDb_consultationLists(
    where: {
      consultationDate: { equals: $consultationDate }
      active: { equals: true }
    }
  ) {
    ...ConsultationListFragment
  }
}
